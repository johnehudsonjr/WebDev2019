// NOTE: Declare a new empty array called newArray
// var newArray = [];

// NOTE: made a for loop that starts at 0 and will end when the counter has the value of 10
//set that counter to increase by 1 every time it enters this loop until the condition is met
// for(counter =0; counter <=10; counter++) {
    // newArray.push (counter)
    // console.log(newArray)
// }



var newArray = [];
    for (counter=0; counter <=100; counter +=2){
    newArray.push(counter)

    if (newArray [20]% 2 == 0 )
{
 console.log("This is even")
}
